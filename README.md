# Next Step: Add a SpringBoot Backend
## Step 1: install all needed configurations
* run npm install axios command to allow http request from backend

## Step 2: Build a Backend Using SpringBoot
* use SpringBoot to build a backend which can transfer data to the frontend
* move all the frontend data into a MySQL database which allow more dynamic data
* solve CORS issue between a VueJS frontend and a SpringBoot backend

## Step 3: Define new Functionalities
* mode switch: enable light and dark mode of the portfolio
* add form component: enable me to dynamically add or modify experiences in different components
* create RESTful APIs: use APIs to transfer data between frontend and backend
* build tests: write unit test functions on backend to test REST APIs
* inject an AI chatbot: can answer questions based on content of the project

## Step 4: Pack and Deployment
* pack the frontend and backend section of the whole project
* deploy the project on AWS ECR and ECS